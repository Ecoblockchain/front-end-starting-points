require(['backbone'],

  	function(Backbone) {

    'use strict';

    console.log('index.js', Backbone);

    // var router = new AppRouter();

    // Backbone.history.start({
    //   root: '/'
    // });

});